name: Release assets

# Recovery path for a partially published release. This deliberately rebuilds
# binaries from the immutable release tag; normal releases still flow through
# release-please.yml.
on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing release tag to rebuild, e.g. v0.1.0"
        required: true
        type: string

permissions:
  contents: read

jobs:
  binaries:
    name: release binaries
    permissions:
      contents: write
      id-token: write
      packages: write
      attestations: write
    uses: rknightion/.github/.github/workflows/binaries.yml@27b0bd31dbe7bf238b89773d00d60ed5dfe187bb # v1.20.0
    with:
      release-tag: ${{ inputs.release_tag }}
      install-cosign: true
      goreleaser-args: "release --clean --skip=docker"
      pre-cmd: "bash .github/scripts/install-syft.sh"
